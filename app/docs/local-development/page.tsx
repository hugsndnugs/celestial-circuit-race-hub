export default function LocalDevelopmentPage() {
  return (
    <main>
      <section className="card">
        <h1>Local Development</h1>
        <p className="muted">Install dependencies once in this merged app and run `npm run dev` to access all modules.</p>
      </section>
    </main>
  );
}
