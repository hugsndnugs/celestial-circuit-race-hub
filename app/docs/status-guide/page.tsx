export default function StatusGuidePage() {
  return (
    <main>
      <section className="card">
        <h1>Status Guide</h1>
        <p className="muted">Service health snapshots are shown at `/status` using the generated `public/status.json` feed.</p>
      </section>
    </main>
  );
}
