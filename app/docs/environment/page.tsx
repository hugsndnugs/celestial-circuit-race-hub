export default function EnvironmentPage() {
  return (
    <main>
      <section className="card">
        <h1>Environment</h1>
        <p className="muted">Use shared `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` for controller and signups modules.</p>
      </section>
    </main>
  );
}
