export default function OperationsRunbookPage() {
  return (
    <main>
      <section className="card">
        <h1>Operations Runbook</h1>
        <p className="muted">Run race operations through `/controller/admin`, marshal logging via `/controller/marshal`, and status checks via `/status`.</p>
      </section>
    </main>
  );
}
