export default function SignupsGuidePage() {
  return (
    <main>
      <section className="card">
        <h1>Signups Guide</h1>
        <p className="muted">Public team intake runs at `/signups` and approved requests can be promoted in `/controller/admin`.</p>
      </section>
    </main>
  );
}
